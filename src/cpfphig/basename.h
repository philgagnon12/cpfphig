#ifndef CPFPHIG_BASENAME_H
#define CPFPHIG_BASENAME_H

#include "cpfphig/cpfphig.h"

cpfphig
cpfphig_basename( const char*                               Path,
                  int                                       Path_Size,
                  char*                                     Buffer,
                  int                                       Buffer_Size,
                  CPFPHIG_OPTIONAL struct cpfphig_error*    Error );

#endif
